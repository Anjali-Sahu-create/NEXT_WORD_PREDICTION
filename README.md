# Next Word Prediction using LSTM

## Overview
<!-- This project predicts the next word in a sentence using an LSTM (Long Short-Term Memory) neural network built with TensorFlow/Keras.

## Features
- Predicts the next word from user input
- Interactive Streamlit web application
- Pre-trained LSTM model
- Tokenizer and maximum sequence length saved using Pickle

## Technologies Used
- Python
- TensorFlow / Keras
- Streamlit
- NumPy
- Pickle

## Project Structure
- app.py
- lsm_model.h5
- tokenizer.pkl
- max_len.pkl

## How to Run

```bash
pip install -r requirements.txt
streamlit run app.py -->